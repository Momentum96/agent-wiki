# Skill Templates

This directory contains the `SKILL.md` templates installed by `agent-wiki setup`.

Expected templates:

- `qmd-cli/SKILL.md`
- `agent-wiki-memory/SKILL.md`

Keep these templates generic. They must not contain user-specific paths, secrets, or raw session data.
