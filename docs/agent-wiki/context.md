# Agent Wiki Context

This wiki is shared by local coding agents.

Agents should use qmd to search this wiki before non-trivial planning or editing. Agents should write structured session logs after meaningful work, then refresh qmd so future sessions can retrieve the result.

Store summaries, decisions, verification notes, and changed-file manifests. Do not store raw transcripts, secrets, environment files, API keys, private keys, or database dumps.
